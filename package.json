{
  "name": "daemux-plugins",
  "private": true,
  "workspaces": [
    "packages/*",
    "channels/*",
    "llm-providers/*",
    "features/*",
    "mcp-servers/*"
  ],
  "scripts": {
    "build": "npm run build:sdk && npm run build:plugins",
    "build:sdk": "npm -w packages/plugin-sdk run build",
    "build:plugins": "npm -w llm-providers/anthropic-provider -w channels/telegram-adapter -w features/human-behavior -w features/transcription -w mcp-servers/codemagic -w mcp-servers/fastlane-mcp run build",
    "clean": "npm -ws --if-present run clean",
    "typecheck": "npm -ws --if-present run typecheck",
    "publish": "./scripts/publish.sh"
  }
}
